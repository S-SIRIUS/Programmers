select b.TITLE, r.BOARD_ID, r.REPLY_ID, r.WRITER_ID, r.CONTENTS, date_format(r.CREATED_DATE, "%Y-%m-%d") as "CREATED_DATE" from USED_GOODS_REPLY r inner join USED_GOODS_BOARD b on r.BOARD_ID = b.BOARD_ID where date_format(b.CREATED_DATE, "%m")=10 order by r.CREATED_DATE, b.TITLE;
