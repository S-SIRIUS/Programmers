
def solution(maps):
    answer = 0
    board = [[0] * len(maps[0]) for _ in range(len(maps))]
    print(board)
    '''
    for mp in maps:
        for m in mp:
            if m==1:
                maps
    '''    
    return answer