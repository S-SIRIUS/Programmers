select p.MEMBER_NAME, r.REVIEW_TEXT, DATE_FORMAT(r.REVIEW_DATE, "%Y-%m-%d") from MEMBER_PROFILE p, (select * from REST_REVIEW where MEMBER_ID = (select MEMBER_ID from REST_REVIEW group by MEMBER_ID order by count(*) desc limit 1)) r where p.MEMBER_ID = r.MEMBER_ID order by r.REVIEW_DATE;